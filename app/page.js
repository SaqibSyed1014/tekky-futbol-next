import HomeClient from './HomeClient';

export const metadata = {
  title: 'TekkyFutbol — For Ballers Who Create',
  description: 'One league. Two divisions. Sixteen weeks to earn your place. Two matches to take the crown.',
};

export default function HomePage() {
  return <HomeClient />;
}
