import ShopClient from './ShopClient';

export const metadata = {
  title: 'The Drip — TekkyFutbol Shop',
  description: 'Season 01 drops built by division — limited pieces for the pitch, the street, and everything in between.',
};

export default function ShopPage() {
  return <ShopClient />;
}
