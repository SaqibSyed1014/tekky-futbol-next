import StreetwearClient from './StreetwearClient';

export const metadata = {
  title: 'TekkyFutbol — Streetwear',
  description: 'Streetwear capsules — limited runs, heavyweight textiles, and custom silhouettes from TekkyFutbol Season 01.',
};

export default function StreetwearPage() {
  return <StreetwearClient />;
}
